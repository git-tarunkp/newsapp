import React from 'react'
import { useNavigate,useNavigation } from "react-router-dom";

const Items = ({title}) => {

    
  return (
    <div>
      {title}
    </div>
  )
}

export default Items
